import 'package:flutter/material.dart';

class SwipingScreen extends StatelessWidget {

  static const routeName = '/swiping_screen';

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Test'),
    );
  }
}
